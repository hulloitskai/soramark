query BookPageQuery($bookId: ID!) {
  book(id: $bookId) {
    authorName
    id
    snaps {
      id
      ...SnapCardSnapFragment
    }
    title
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
