query BookPageQuery($bookId: ID!) {
  book(id: $bookId) {
    authorName
    id
    snaps {
      id
      photo {
        id
        url
      }
    }
    title
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
